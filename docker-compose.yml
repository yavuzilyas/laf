version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      # Host makinede kalıcı uploads klasörü
      - /var/lib/dokploy/volumes/laf-uploads:/app/uploads
    environment:
      - UPLOAD_DIR=/app/uploads
    restart: unless-stopped
